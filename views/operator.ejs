<!DOCTYPE html>
<html>
<head>
  <title>Operator Dashboard</title>
</head>
<body>
  <h1>Operator Dashboard</h1>
  <p>Total vouchers sold today: <%= totalSoldToday %></p>

  <!-- Profile selection -->
  <form method="POST" action="/operator/create-voucher">
    <label for="profile">Choose profile:</label>
    <select name="profile" id="profile">
      <option value="1h">1 Hour</option>
      <option value="day">1 Day</option>
      <option value="week">1 Week</option>
    </select>
    <button type="submit">Create Voucher</button>
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
  </form>

  <% if (voucher) { %>
    <div>
      <p>Voucher Username: <%= voucher.username %></p>
      <p>Price: <%= voucher.price %> XOF</p>
      <form method="POST" action="/operator/sell-voucher">
        <input type="hidden" name="voucherId" value="<%= voucher.id %>">
        <label for="amount">Enter amount:</label>
        <input type="number" name="amount" required>
        <button type="submit">Sell</button>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      </form>
    </div>
  <% } %>

  <% if (voucherSold) { %>
    <div>
      <p>Voucher Password: <%= voucherSold.password %></p>
      <form method="POST" action="/operator/close-sale">
        <input type="hidden" name="voucherId" value="<%= voucherSold.id %>">
        <button type="submit">Close Transaction</button>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      </form>
    </div>
  <% } %>

  <% if (error) { %>
    <p style="color:red;"><%= error %></p>
  <% } %>
</body>
</html>

