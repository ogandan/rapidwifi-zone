<!DOCTYPE html>
<html>
<head>
  <title>Login Result</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body class="container mt-5">

  <% if (success) { %>
    <div class="alert alert-success">
      ✅ Login Successful
    </div>
  <% } else { %>
    <div class="alert alert-danger">
      ❌ Login Failed
    </div>
    <a href="/login" class="btn btn-primary">Retry</a>
  <% } %>

  <!-- Conditional links: only show if role is admin or operator -->
  <% if (user && user.role === 'admin') { %>
    <a href="/admin" class="btn btn-secondary mt-3">Go to Admin Dashboard</a>
  <% } else if (user && user.role === 'operator') { %>
    <a href="/operator" class="btn btn-secondary mt-3">Go to Operator Dashboard</a>
  <% } %>

</body>
</html>

